import {
  expect
} from 'chai'

import {
  Signals
} from 'shinkansen-signals'

describe('shinkansen-signals', () => {
  it('is an object', () => {
    expect(Signals)
      .to.be.an('object')
  })
})
